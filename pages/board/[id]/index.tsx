import Head from 'next/head';

const Index: React.FC = () => {
    return (
        <div>
            <Head>
                <title>Create Next App</title>
            </Head>
            board/:id
        </div>
    );
};

export default Index;
